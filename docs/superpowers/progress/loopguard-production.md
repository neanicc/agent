# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is ready to begin
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T01`
- Last completed step: `INTEGRATIONS-T08` completed inline. LoopGuard now reports managed/attached capabilities without overclaiming cloud support, provides guided project-scoped setup, compatibility diagnostics, DX metrics and redacted support bundles, and installs the daemon as a hardened per-user macOS LaunchAgent or Linux systemd service. Windows current-user Scheduled Task support is explicit and experimental. Install, upgrade, rollback, status, synthetic verification, uninstall, and data-purge protections are idempotent and fail closed around ownership, permissions, and symlinks.
- Expected/observed last commit: Expected and observed task commit `40f4fe4 feat: start loopguard daemon as a user service`; predecessor `653e692 feat: report agent integration capabilities`.
- Task dependencies and owned files: Managed integrations are complete through `INTEGRATIONS-T08`. `ROUTING-T01` next introduces immutable provider-neutral catalog types and signed/discovered model metadata without embedding a permanent latest model or treating unknown price as zero.
- Red/green/regression commands with exit codes and counts: T07 and T08 completed their focused adapter/control gates with 384 and 402 passing tests respectively. The tranche completion gate exited 0 with 653 Python tests, 3 Node bridge tests, strict TypeScript, Ruff lint, generated CLI docs, `plutil`, isolated wheel/template packaging, and a production npm audit with 0 vulnerabilities. Live read-only Codex and Claude probes both returned stable `not_installed` capability reports; the Codex probe required normal local configuration access rather than the filesystem sandbox. Repository-wide Ruff formatting still reports the recorded 65-file historical baseline, so only task-owned files are format-checked and no unrelated mass rewrite is performed.
- Evidence/artifact paths: `loopguard/src/loopguard/adapters/{doctor.py,setup.py,service_install.py}`, `loopguard/src/loopguard/adapters/templates/{com.loopguard.daemon.plist,loopguardd.service,loopguardd-windows.xml}`, `loopguard/tests/adapters/{test_doctor.py,test_setup.py,test_service_install.py,test_service_install_windows.py}`, `loopguard/docs/{getting-started-protected-session.md,integration-compatibility.md}`, and generated CLI/config/error references.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — full Python suite 653 passed, Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Read all of `ROUTING-T01`, add the failing catalog availability/trust/freshness/unknown-price tests, confirm the missing package failure, then implement and commit `feat: add provider-neutral model catalog`.
