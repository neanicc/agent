# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T05`
- Last completed step: `HEAL-T05` completed inline. The deterministic planner emits three fixed repair strategies from one immutable failure/revision/evidence envelope. Every candidate starts from its own exact detached worktree, then the managed generator sees only an allowlisted file projection with no Git metadata or unrelated repository context. Generation requires attested read/write scoping, disabled network, hidden credentials, no host/cloud/publishing access, valid Codex/Claude sandbox values, and bounded files, context bytes, patch bytes, changed lines, tool calls, wall time, tokens, and cost. The service distrusts generator claims, independently inspects Git, denies binary/lock/CI/secret/infra/migration/symlink/submodule changes unless the explicit policy permits the applicable class, and persists accepted patches plus model evidence in a private content-addressed store before cleanup.
- Expected/observed last commit: Expected task commit `feat: generate isolated repair candidates`; observed dependency commit `4ed62ca feat: reproduce pipeline failures in isolation`.
- Task dependencies and owned files: `HEAL-T05` consumes T02 normalized failures, T03 bounded evidence, T04 exact-revision worktrees, and the earlier deterministic model/effort router plus managed-agent adapter contract. It owns repair strategy briefs, hostile-evidence framing, candidate budgets/policy, restricted projections, managed isolation evidence, patch admission, and local content-addressed artifact staging.
- Red/green/regression commands with exit codes and counts: Candidate tests first failed during collection because the planner/generator/candidate modules were absent. All 46 heal tests pass with one opt-in Docker integration skipped because no local Docker runtime/image is configured. The focused managed Codex/Claude adapter and routing regression gate adds 63 passing tests. Strict Ruff and formatting checks pass. Coverage includes independent baselines/worktrees, generator-claim distrust, persisted 0600 artifacts, malicious instructions in every evidence field, protected and explicitly permitted patch classes, binary and line/byte budgets, isolation-attestation failure, tool/cost budget failure, projection context exclusion, and fixed strategy ordering.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/planner.py`, `loopguard/src/loopguard/heal/generator.py`, `loopguard/src/loopguard/heal/candidates.py`, and `loopguard/tests/heal/test_candidates.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, sandbox admission/teardown tests, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T05` as `feat: generate isolated repair candidates`; then begin `HEAL-T06` with ordered replay/data-quality/regression/security evaluation and deterministic ranking tests.
