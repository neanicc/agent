# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T05`
- Last completed step: `CLOUD-T05` completed inline. Browser stream tickets use 256-bit randomness, hashed lookup values, a 30-second window, atomic single use, and exact tenant/user/session/origin/starting-cursor bindings. Native clients authenticate with a bearer header during the upgrade, never a token in the URL. Session streams authorize before acceptance, replay durable rows before waiting for notifications, fill gaps by reading after the last `session_seq`, assign a separate per-connection `client_stream_seq`, emit resumable heartbeat metadata, bound sends, ignore duplicate notifications through durable cursor reads, and fail closed on deleted or cross-session state.
- Expected/observed last commit: Expected task commit `feat: stream sessions with cursor replay`; observed prior task commit `6c9f52d feat: pair hosts and relay durable events`.
- Task dependencies and owned files: `CLOUD-T05` establishes resumable browser/native observation semantics. `CLOUD-T06` will replace the T03 action authorization seam with challenged, device-signed, cloud-countersigned, expiring, at-most-once actions and daemon-side verification.
- Red/green/regression commands with exit codes and counts: T05 RED exited 2 on absent ticket/subscription modules; native bearer RED then closed with 4401 before implementation. GREEN focused stream tests exited 0 with four replay/live, ticket lifecycle, cross-session/deletion, and native-header cases. The complete service gate exited 0 with 41 passed and one Docker-only PostgreSQL RLS skip; Ruff, Alembic drift, and diff checks exited 0. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/src/loopguard_api/{stream_tickets.py,subscriptions.py,routes/sessions.py,app.py}` and `services/control-api/tests/test_session_stream.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T05` as `feat: stream sessions with cursor replay`; then begin `CLOUD-T06` with failing expiry, replay, stale-state, device-proof, lifecycle, revocation, unknown-key, and acknowledgement-loss tests.
