# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; repair-contract activation remains
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T11`
- Last completed step: `SURFACE-T11` completed inline. The Next.js console now has an immutable-build health contract and a pinned, standalone, non-root container that is exercised read-only. The native archive embeds an accurate privacy manifest and uses credential-free App Store Connect export settings. Ordinary CI tests and packages both clients, creates checksums plus web SBOM/provenance evidence, and cross-verifies the artifacts. Environment-protected manual jobs may sign and publish the web image by digest or upload a signed iOS build to TestFlight, but neither deploys the image nor submits App Review.
- Expected/observed last commit: Expected task commit `ops: package web and ios client releases`; observed prior task commit `03115e2 test: enforce control surface quality`.
- Task dependencies and owned files: `SURFACE-T11` consumes the complete T01–T10 web/native clients and their existing test gates. It owns only client packaging, health, privacy/export declarations, verification automation, and operator documentation; no registry, deployment target, signing credential, TestFlight build, or App Store submission was mutated.
- Red/green/regression commands with exit codes and counts: The health test first failed because the route did not exist; it now passes in the full 9-file/34-test Vitest suite alongside strict TypeScript, zero-warning ESLint, and the production Next.js build. The artifact verifier first failed closed on the absent immutable image reference, then passed against locally assembled checksum fixtures and a real unsigned archive. `xcodebuild archive` succeeded for generic iOS Release with warnings-as-errors; archive bundle `dev.loopguard.ios`, version `0.1.0`, and embedded `PrivacyInfo.xcprivacy` were independently checked. Both plists pass `plutil`; `actionlint`, `shellcheck`, workflow YAML parsing, `git diff --check`, and development/production `npm audit` with zero vulnerabilities all pass. A local Docker build was not possible because this workstation has no container runtime; the workflow contains an explicit read-only container health smoke test as an external CI gate.
- Evidence/artifact paths: `apps/web/Dockerfile`, `apps/web/src/app/health/route.test.ts`, `apps/ios/build/LoopGuard.xcarchive` (ignored local evidence), `apps/ios/LoopGuard/Resources/PrivacyInfo.xcprivacy`, `apps/ios/ExportOptions.plist`, `.github/workflows/client-release.yml`, `scripts/verify_client_release.sh`, and `docs/operations/client-release.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `SURFACE-T11` as `ops: package web and ios client releases`; then begin Auto-Heal `HEAL-T01`, because `SURFACE-T12` explicitly depends on completion of Auto-Heal Tasks 1–9.
