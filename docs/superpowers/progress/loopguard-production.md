# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T04`
- Last completed step: `CLOUD-T04` completed inline. Host pairing codes use 256-bit randomness, SHA-256-only stored lookup material, five-minute expiry, and atomic one-use consumption. Hook credentials are returned once, stored as server-derived HMAC keys, repository/host scoped, expiring and revocable; verification binds the canonical method, path, UTC timestamp, nonce, repository handle, and exact body SHA-256 while pruning and rejecting replayed nonces. Relay batches are bounded, contiguous, topology checked, duplicate-safe, and model event/outbox persistence under one lock. The local daemon now has a durable schema-v3 per-repository relay checkpoint, bounded uploads, strict acknowledgement validation, partial-ack recovery, reconnect backoff, and never deletes local events after cloud acknowledgement.
- Expected/observed last commit: Expected task commit `feat: pair hosts and relay durable events`; observed prior task commit `6591e5c feat: secure control api with oidc roles`.
- Task dependencies and owned files: `CLOUD-T04` establishes repository-bound event delivery and the durable local/cloud cursor handshake. `CLOUD-T05` will turn persisted session events and the outbox into replay-before-live browser/native streams with one-use origin-bound tickets.
- Red/green/regression commands with exit codes and counts: T04 RED exited 2 on absent pairing, hook, relay, and local cloud-relay modules. GREEN exited 0 for six control-service pairing/hook/relay cases and three local reconnect/checkpoint/batching cases. The affected local event-store/recovery/dispatch gate passed 70 tests; the service gate passed 37 with one Docker-only skip; service and local Ruff checks and Alembic drift check exited 0. The broad local suite reached 821 passed with one transient real broker startup failure; that exact broker smoke passed immediately in isolation, and no relay/store regression remained. The only Python warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/src/loopguard_api/{pairing.py,relay.py,hook_ingest.py,routes/hosts.py,routes/hooks.py}`, `services/control-api/tests/{test_pairing_relay.py,test_hook_ingest.py}`, `loopguard/src/loopguard/control/{cloud_relay.py,migrations.py,store.py}`, and `loopguard/tests/control/test_cloud_relay.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T04` as `feat: pair hosts and relay durable events`; then begin `CLOUD-T05` with failing replay-before-live, ticket expiry/reuse/origin, gap fill, duplicate notification, and slow-client tests.
