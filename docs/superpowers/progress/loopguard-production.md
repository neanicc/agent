# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 7 of 13 complete — tranche 8 browser acceleration is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-browser-acceleration.md`, `BROWSER-T01`
- Last completed step: `BROWSER-T01` completed inline. The new pinned Node 20+ broker package has a strict Zod discriminated protocol for context creation/closure, bounded page actions, and health. It requires an exact version plus high-entropy daemon capability handshake, rejects unknown fields, validates secret-safe response envelopes, and uses fatal-UTF-8, length-prefixed frames with a 1 MiB frame bound and per-push work cap. Context capabilities are required to be high entropy rather than sequential or guessable.
- Expected/observed last commit: Expected task commit `feat: define isolated browser broker protocol`; observed predecessor `b03089e feat: include design preferences in verification`.
- Task dependencies and owned files: `BROWSER-T01` establishes the only wire contract consumed by the T02 broker runtime and T03 Python supervisor. Package versions and the npm resolution are locked; no browser runtime is launched in this protocol-only task.
- Red/green/regression commands with exit codes and counts: T01 RED first hit the expected sandbox-only tsx IPC denial, then exited 1 under permitted execution with the expected missing `protocol.js`. GREEN exited 0 with 6 protocol/frame tests and strict TypeScript exited 0. npm audit reports 0 vulnerabilities. The preceding preference-plan completion gate exited 0 with 48 tests.
- Evidence/artifact paths: `loopguard/integrations/browser-broker/{package.json,package-lock.json,tsconfig.json,src/protocol.ts,src/protocol.test.ts}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `BROWSER-T01` as `feat: define isolated browser broker protocol`, then begin `BROWSER-T02` with failing lifecycle, isolation, origin-policy, capability, cleanup, and crash tests.
