# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; next planned tranche is Auto-Heal
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T09`
- Last completed step: `CLOUD-T09` completed inline. Migration `0002` adds forced-RLS preference profiles and observed usage records with stable uniqueness/time indexes. Versioned routes now own effective capabilities, hosts, sessions, changes, verifications, actions, repairs, artifacts, preferences, costs, devices, stream tickets, and audit. List/detail operations are tenant-safe; repair remains explicitly unavailable until Auto-Heal. Managed safety rules cannot be weakened, cost categories are exact observed decimals with no invented avoided-cost estimate, and capability availability is the fail-closed intersection of role, host freshness, adapter observation, and repository binding. Device pairing uses hashed five-minute one-use challenges and verified Ed25519/P-256 possession; revocation propagates to queued actions. A checked contract maps every client operation to permission, idempotency/state, cursor domain, and owner, and generated OpenAPI must contain every operation with unique IDs.
- Expected/observed last commit: Expected task commit `feat: expose capability-aware control APIs`; observed prior task commit `3eab516 feat: add durable workflows and immutable audit`.
- Task dependencies and owned files: `CLOUD-T09` completes the cloud plan and freezes the API surface consumed by web/iOS and the later Auto-Heal workflow. The next tranche should follow the implementation prompt ordering and preserve these ownership/capability boundaries.
- Red/green/regression commands with exit codes and counts: T09 RED exited 2 on the absent query/device modules; the first OpenAPI run then failed on an unresolved postponed UUID annotation and passed after restoring its source import. GREEN focused query/matrix tests passed seven. The complete service gate exited 0 with 63 passed and one Docker-only PostgreSQL RLS skip; Ruff and Alembic drift checks passed. SQLite base/head round-trip, PostgreSQL offline compilation through `0002`, diff check, and isolated sdist/wheel build all exited 0. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/alembic/versions/0002_control_queries.py`, `services/control-api/src/loopguard_api/{client_queries.py,device_pairing.py,routes/}`, `services/control-api/tests/{test_control_queries.py,test_client_api_matrix.py}`, and `contracts/control-api-endpoints.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T09` as `feat: expose capability-aware control APIs`; then re-read the implementation prompt and begin the next incomplete plan/tranche at its first immutable task ID.
