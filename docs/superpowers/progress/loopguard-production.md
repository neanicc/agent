# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T04`
- Last completed step: `HEAL-T04` completed inline. Reproduction now materializes an exact detached Git revision, binds repository identity/SHA before execution, and accepts only a freshly normalized observed failure whose fingerprint exactly matches intake. Sandbox evidence records the immutable image digest, command, repository SHA, exit status, bounded output artifact, validated artifact paths, fingerprint, and assurance level. Local Docker is explicitly lower-assurance/opt-in and requires an already-present allowlisted digest; the untrusted container is non-root, read-only, offline, capability-free, non-privileged, PID/CPU/memory/time/tmp/output bounded, and receives only read-only source/fixture plus one bounded artifact mount. Hosted results require gVisor/Kata/Firecracker evidence for rootless runtime, signed/provenanced image, seccomp/AppArmor, no host namespace/path/socket/service token/metadata/network, enforced deadline, and bounded resources.
- Expected/observed last commit: Expected task commit `feat: reproduce pipeline failures in isolation`; observed prior task commit `f3e624b feat: build redacted pipeline replay fixtures`.
- Task dependencies and owned files: `HEAL-T04` consumes T02 fingerprints and T03 safe fixtures. It owns repair-specific sandbox/runtime/reproduction contracts, an exact-revision worktree lifecycle, local Docker adapter, archive/artifact policy, and a coordinate failure fixture. Hosted execution remains an injected runtime but cannot claim `hosted_isolated` without complete isolation evidence.
- Red/green/regression commands with exit codes and counts: Reproduction tests first failed during collection because the sandbox/runtime modules were absent. All 31 heal tests pass with one opt-in Docker integration skipped because no local Docker runtime/image is configured. The 21 existing worktree and verification-runner isolation/resource/cleanup tests also pass. Strict Ruff and formatting checks pass. Coverage includes matching and unrelated fingerprints, successful/non-observed/timeout/invalid runs, repository substitution, oversized hostile output, allowlisted digest enforcement, rootless/offline/read-only container options, hosted strong-boundary evidence, cleanup failure, symlink/device/archive traversal and bomb rejection, forbidden secret environment/shell commands, and exact detached worktree teardown.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/sandbox.py`, `loopguard/src/loopguard/heal/docker.py`, `loopguard/src/loopguard/heal/reproduce.py`, `loopguard/tests/heal/test_reproduce.py`, and `loopguard/tests/fixtures/heal/coordinate_pipeline/`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, sandbox admission/teardown tests, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T04` as `feat: reproduce pipeline failures in isolation`; then begin `HEAL-T05` with independent baseline worktrees, bounded generation budgets, and adversarial patch-scope tests.
