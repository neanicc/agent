# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T07`
- Last completed step: `CLOUD-T07` completed inline. Artifact initiation validates bounded declarations, allocates tenant-prefixed opaque keys, requests per-artifact envelope metadata from KMS, and signs upload requirements for SHA-256, byte count, media type, expiry, and encryption. Completion requires a signed evidence manifest, exact provider-reported size/type/checksum, or a bounded server recomputation when a provider lacks trustworthy checksums; ETag is never trusted. Downloads are tenant-safe, complete/unexpired only, short lived, and audited. The retention worker respects audited legal holds, records deletion state, and retries object-store failures. Hosted startup refuses implicit development object/KMS adapters.
- Expected/observed last commit: Expected task commit `feat: store encrypted evidence artifacts`; observed prior task commit `eda3098 feat: execute signed expiring remote actions`.
- Task dependencies and owned files: `CLOUD-T07` establishes evidence object integrity and retention semantics. `CLOUD-T08` will add deterministic workflow IDs/idempotent activities, notification retry, append-only audit enforcement, and the documented retention/de-identification matrix.
- Red/green/regression commands with exit codes and counts: T07 RED exited 2 on the absent artifact module. GREEN focused artifact/error cases passed 12, covering tenant isolation, real checksum versus multipart ETag, bounded checksum fallback, deletion retry, and legal hold. The complete service gate exited 0 with 49 passed and one Docker-only PostgreSQL RLS skip; Ruff, Alembic drift, and diff checks exited 0. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/src/loopguard_api/{artifacts.py,routes/artifacts.py,app.py,errors.py}`, `services/control-api/tests/test_artifacts.py`, and `docs/reference/control-api-errors.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T07` as `feat: store encrypted evidence artifacts`; then begin `CLOUD-T08` with failing notification retry/idempotency, workflow replay, immutable audit, retention, legal-hold, and tenant-deletion tests.
