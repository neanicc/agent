# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T01`
- Last completed step: `SURFACE-T01` completed inline. The control API now exports a deterministic versioned OpenAPI snapshot and generated reference, refuses same-major removal of operations, required response fields, or enum members, and ships shared fixtures for replay/gap handling, action lifecycles, disabled/ready capabilities, and host/integration health. The implementation-prompt handoff was corrected: core control-surface Tasks 1–11 precede Auto-Heal, while repair routes remain capability-hidden until Surface Task 12.
- Expected/observed last commit: Expected task commit `feat: publish versioned control api contract`; observed prior task commit `bd7eeb8 feat: expose capability-aware control APIs`.
- Task dependencies and owned files: `SURFACE-T01` freezes the generated client input consumed by web Task 2 and iOS Task 5. `SURFACE-T02` may scaffold the web BFF/client and locked design system but must not expose repair navigation while the effective repair capability is disabled.
- Red/green/regression commands with exit codes and counts: T01 RED passed the route-presence assertion and failed three assertions because the exporter, checked snapshots, compatibility checker, and fixtures were absent. GREEN passed four focused contract tests. A second export produced identical SHA-1 values for both artifacts; Ruff passed; the complete control-service suite exited 0 with 67 passed and one Docker-only PostgreSQL RLS skip. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/scripts/export_openapi.py`, `services/control-api/tests/test_openapi_contract.py`, `contracts/control-api.openapi.json`, `contracts/fixtures/`, and `docs/reference/control-api.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T01` as `feat: publish versioned control api contract`; then begin `SURFACE-T02` with failing browser/server client tests and the locked cross-platform design-system document.
