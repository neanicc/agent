# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 3 of 13 — regression verification and proof contracts
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-regression-verification.md`, `VERIFY-T01`
- Last completed step: `INTEGRATIONS-T04` completed inline with a strict-valid Claude Code plugin, stable local marketplace, exact user/project fallback merging, administrator-policy detection, ownership-safe uninstall, CLI lifecycle, and a signed HTTPS local-first fallback that never commits credentials or treats `CLAUDE_CODE_REMOTE` as authentication. A review also fixed the shared normalizer so the declared `PreCompact` hooks produce a dedicated `context.compacted` event rather than failing open as unsupported; Claude remote execution is now explicitly labeled. Plugin and fallback activation are mutually exclusive, and cloud delivery remains `conditional` until a real endpoint smoke test.
- Expected/observed last commit: Expected task commit subject `feat: install claude local and cloud hooks`; observed predecessor `c548a65 feat: install codex lifecycle hooks`.
- Task dependencies and owned files: `VERIFY-T01` follows the completed foundation and attached-integration tranche; it owns the proof-contract schema and fixtures identified by regression-verification Task 1 and must preserve the new event/integration contracts.
- Red/green/regression commands with exit codes and counts: T04 installer RED exited 2 during collection because `claude_hooks` was absent; cloud canonical-signing RED failed on the exact event schema before correction; `PreCompact`/remote-label RED had 3 expected failures before normalization support. Focused Claude installer/CLI GREEN exited 0 with 10 passed; hook-entry focused GREEN exited 0 with 23 passed when the sandbox-only live socket case was excluded; projection GREEN exited 0 with 54 passed. Claude strict plugin validation, generated CLI drift check, Ruff, and diff checks exited 0. An isolated Claude Code 2.1.210 marketplace install/reinstall/verify/uninstall/re-uninstall cycle matched expected active/healthy/idempotent states. The escalated warning-strict cumulative suite exited 0 with 402 passed. Offline sdist/wheel build exited 0 with the Claude fallback template included; isolated build dependency resolution failed only because network access was unavailable and the installed build backend is older than the declared build requirement.
- Evidence/artifact paths: `loopguard/src/loopguard/adapters/claude_hooks.py`, `loopguard/src/loopguard/adapters/cloud_hook_client.py`, `loopguard/integrations/claude-plugin`, `loopguard/src/loopguard/adapters/templates/claude-settings-fallback.json`, `loopguard/tests/adapters/test_claude_install.py`, `loopguard/tests/adapters/test_hook_entry.py`, generated CLI reference, and isolated live runtime evidence under `/tmp/loopguard-claude-plugin.QjNMjK`.
- Last successful cumulative verification: 2026-07-15 — full warning-strict Python suite 402 passed, full Ruff clean, Claude plugin and materialized marketplace schema validation clean, live install lifecycle healthy, generated CLI reference current, offline wheel/sdist build clean with templates included, and diff whitespace clean, with the single recorded external Starlette deprecation only.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed.
- Active blockers: None.
- Next exact action: Commit `INTEGRATIONS-T04` as `feat: install claude local and cloud hooks`, verify a clean tree and commit subject, then read the complete regression-verification plan and begin `VERIFY-T01` with its proof-contract schema RED tests.
