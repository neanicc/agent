# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-preference-engine.md`, `PREFERENCES-T01`
- Last completed step: `PREFERENCES-T01` completed inline. Preference rules, profiles, evidence, and verdicts are strict immutable versioned contracts with bounded identifiers, text, scopes, JSON metadata, and confidence. Profiles reject duplicate rule IDs and support deterministic lookup. Every evidence/verdict record cites its rule, artifact, evaluator, and evaluator version; learned rules cannot carry blocking severity.
- Expected/observed last commit: Expected task commit `feat: define editable preference policy`; observed tranche milestone `99443aa docs: record model routing milestone`.
- Task dependencies and owned files: `PREFERENCES-T01` is a standalone contract layer consumed by the compiler, evaluators, learner, critic, and verification integration. `PREFERENCES-T02` next owns untrusted repository parsing, source precedence, manifests, fixtures, and optional dependency metadata.
- Red/green/regression commands with exit codes and counts: T01 RED exited 2 during collection with the expected missing `loopguard.preferences` package. GREEN exited 0 with 5 contract tests covering severity boundaries, duplicate IDs, evidence citations, strict extras, path-like identifiers, and oversized JSON. Ruff exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/preferences/{__init__.py,models.py}` and `loopguard/tests/preferences/test_models.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `PREFERENCES-T01` as `feat: define editable preference policy`, then begin `PREFERENCES-T02` with failing precedence and malformed-source tests.
