# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T06`
- Last completed step: `HEAL-T06` completed inline. Evaluation re-reads and hashes the content-addressed patch, verifies its admitted file/line metadata, applies it cleanly to a new exact-revision worktree, then executes reproduction, fixture/schema, data-quality, impacted tests, security, and static checks in fixed order through the existing offline/resource-bounded verifier. Required failure stops later work; timeout, skipped, runner error, contract-analysis error, and unavailable isolation all fail closed as inconclusive. Contract deltas and evidence are retained. Ranking contains no model call and filters every incomplete/failing/breaking candidate before sorting by nonbreaking contract changes, risk, changed files, changed lines, duration, and stable ID.
- Expected/observed last commit: Expected task commit `feat: rank repairs by deterministic evidence`; observed dependency commit `9c236a8 feat: generate isolated repair candidates`.
- Task dependencies and owned files: `HEAL-T06` consumes T04 worktree isolation, T05 content-addressed patches, the existing verification command runner/isolation providers, and T03 schema delta contracts. It owns the ordered repair evaluation plan, runner adapter, candidate evaluation evidence, and deterministic rank result.
- Red/green/regression commands with exit codes and counts: Evaluation tests first failed during collection because evaluation/ranking modules were absent. All 52 heal tests pass with one opt-in Docker integration skipped. The focused worktree, verification-runner, managed Codex/Claude adapter, and router regression gate adds 84 passing tests. Strict Ruff and formatting checks pass. Coverage includes exact patch application, required stage ordering, timeout short-circuit/inconclusive behavior, artifact tampering, deterministic filtering/tie-breaking, and contract-breaking rejection.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/evaluate.py`, `loopguard/src/loopguard/heal/rank.py`, `loopguard/src/loopguard/heal/models.py`, `loopguard/src/loopguard/heal/candidates.py`, and `loopguard/tests/heal/test_evaluate.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, sandbox admission/teardown tests, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T06` as `feat: rank repairs by deterministic evidence`; then begin `HEAL-T07` with draft-only GitHub App publication, expected-object ref protection, idempotency reconciliation, and evidence-rich report tests.
