# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-preference-engine.md`, `PREFERENCES-T04`
- Last completed step: `PREFERENCES-T04` completed inline. Owner-only versioned SQLite records idempotent operator decisions by tenant/user/repository, authenticated actor, flat bounded context, optional artifact hash, and canonical payload hash. Conservative derivation requires minimum consistent examples and confidence; conflicts and negative examples lower confidence. Learned rules begin as `inform`; only audited explicit promotion returns `warn`/`block` with source `explicit`. Revocation and deletion physically remove contributing payloads, preserve minimal scoped actor/reason/hash tombstones, reject event replay, and deletion contributes durable negative evidence before future relearning.
- Expected/observed last commit: Expected task commit `feat: learn reviewable soft preferences`; observed predecessor `9c98c15 feat: evaluate deterministic design preferences`.
- Task dependencies and owned files: `PREFERENCES-T04` consumes T01 rule contracts and supplies scoped learned/promoted rules to the compiler/service. Default local operation binds to the authenticated OS actor; multi-tenant callers must pass explicit actor and scope contracts. `PREFERENCES-T05` next owns bounded hostile-input-safe visual criticism and cost eligibility.
- Red/green/regression commands with exit codes and counts: T04 RED exited 2 during collection with the expected missing learning module. GREEN and full preference regression exited 0 with 33 tests covering consistency/conflict thresholds, scoping, artifact hashes, idempotent replay, revocation, deletion/negative evidence, promotion, bounded contexts, owner-only/symlink safety, and lookalike-schema rejection. Ruff and diff whitespace checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/preferences/{store.py,learning.py,__init__.py}` and `loopguard/tests/preferences/test_learning.py`, plus the prior preference suite.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `PREFERENCES-T04` as `feat: learn reviewable soft preferences`, then begin `PREFERENCES-T05` with failing visual-critic eligibility, strict-schema, prompt-injection, severity-clamp, and budget tests.
