# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-preference-engine.md`, `PREFERENCES-T03`
- Last completed step: `PREFERENCES-T03` completed inline. A deterministic offline registry evaluates bounded typed artifacts for unknown design-token literals, required shared components/imports, axe findings, web/iOS accessible names, complete iOS accessibility snapshots, Dynamic Type/categories/clipping, reduced motion, and screenshot presence/dimensions/references. Verdict IDs and confidence are replay-stable and cite the rule/artifact/evaluator/version. Malformed evidence becomes inconclusive, while a configured but unavailable evaluator fails closed instead of silently dropping a hard rule; no generic aesthetic score or model call exists.
- Expected/observed last commit: Expected task commit `feat: evaluate deterministic design preferences`; observed compiler commits `1d8e484 feat: compile repository and user preferences` and `dcc4158 test: add hostile preference instruction fixture`.
- Task dependencies and owned files: `PREFERENCES-T03` consumes T01 profiles and T02 compiled tokens. Its artifact/verdict contracts feed T06 verification and its deterministic results can be used without installing or calling a model. `PREFERENCES-T04` next owns authenticated scoped decision recording, conservative derivation, promotion, revocation, deletion, and audit tombstones.
- Red/green/regression commands with exit codes and counts: T03 RED failed while loading conftest with the expected missing evaluator module. GREEN and preference regression exited 0 with 24 tests covering every initial evaluator, malformed/incomplete evidence, iOS and web accessibility, deterministic replay, and unknown-registry failure. Ruff and diff whitespace checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/preferences/{evaluators.py,accessibility.py,__init__.py}`, `loopguard/tests/preferences/{conftest.py,test_evaluators.py}`, plus the T01/T02 preference suite.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `PREFERENCES-T03` as `feat: evaluate deterministic design preferences`, then begin `PREFERENCES-T04` with failing consistent-choice derivation and conflicting-choice tests.
