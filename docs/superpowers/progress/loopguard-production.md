# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T07`
- Last completed step: `SURFACE-T07` completed inline. The native app now exposes only implemented capability destinations—Inbox, Runs, Changes, and Settings—with no premature Repairs tab. Inbox deterministically prioritizes regressions, possible loops, approvals, and failed verification before quiet completions. Run and Change detail put current context, provenance, and verification proof before compact timelines/diffs. Settings owns account, paired-device security, host/integration diagnostics and local setup commands, attention notifications, privacy/local-only preference, and version. Timeline ingestion deduplicates replay, advances client cursors, queues gaps, and visibly resyncs. Every surface has explicit loading, empty, scoped-error, success, stale/offline, and resync fixtures.
- Expected/observed last commit: Expected task commit `feat: add ios safety inbox and run timeline`; observed prior task commit `cafe130 feat: add client authentication and device pairing`.
- Task dependencies and owned files: `SURFACE-T07` consumes the T05 native shell, T06 OIDC/device proof, and cloud collection contracts; it publishes typed monitoring models, capability-aware navigation, deterministic fixture scenarios, and proof-first native detail surfaces for T08 actions/notifications and T12 Repairs. It does not add or imply action capabilities.
- Red/green/regression commands with exit codes and counts: Timeline/inbox RED failed because the native reducer and inbox model were absent. Final signed `xcodebuild test` on iPhone 17 Pro / iOS 26.5 passed all 21 tests (16 unit, 5 UI) under Swift 6 strict concurrency and warnings-as-errors. UI coverage exercises loading, empty, scoped error/request ID, stale/offline, resync, priority, no dead/premature tab, proof-first run/change detail, Settings, device security, and navigation. The selected Runs/Changes journey also passed on iPad Pro 11-inch / iOS 26.4.1. The source Xcode scheme disables parallel test bundles to prevent same-app simulator launch races.
- Evidence/artifact paths: `apps/ios/LoopGuard/App/MainTabView.swift`, `apps/ios/LoopGuard/Features/Inbox/`, `apps/ios/LoopGuard/Features/Runs/`, `apps/ios/LoopGuard/Features/Changes/`, `apps/ios/LoopGuard/Features/Settings/`, `apps/ios/LoopGuardUITests/`, `/private/tmp/loopguard-t07-inbox-light.png`, `/private/tmp/loopguard-t07-inbox-dark.png`, `/private/tmp/loopguard-t07-inbox-largest-type-final.png`, and `/private/tmp/loopguard-t07-ipad-attachments/`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T07` as `feat: add ios safety inbox and run timeline`; then begin `SURFACE-T08` with failing expiry, duplicate-submit, exact-payload signing, and notification/deep-link tests.
