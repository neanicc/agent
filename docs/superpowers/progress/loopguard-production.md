# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T01`
- Last completed step: `CLOUD-T01` completed inline. The separately packageable FastAPI service now has an app factory, public build-only health response, immutable `pydantic-settings` configuration, and no usable production defaults: PostgreSQL, OIDC, external active/verification signing keys, signed hooks, object storage/KMS, Temporal, exact HTTPS origins, hosts, and proxy networks must be explicit. A single request boundary enforces generated/sanitized request IDs, actual/declared body limits, deadlines, exact hosts/origins, trusted forwarded headers, cookie CSRF proof, credential-safe CORS, and metadata-only structured logs. Framework, validation, policy, and unexpected failures use the documented RFC 9457 problem shape without echoing rejected values or internal exception text.
- Expected/observed last commit: Expected task commit `feat: scaffold hosted control api`; observed checkpoint commit `d8a2ef2 test: stabilize cumulative suite collection`.
- Task dependencies and owned files: `CLOUD-T01` establishes the stable public error/settings boundary required by all later cloud routes. `CLOUD-T02` will add tenant-context-required SQLAlchemy repositories, PostgreSQL RLS migrations, distinct cursor domains, and idempotent event ingest.
- Red/green/regression commands with exit codes and counts: T01 RED exited 2 with the expected missing `loopguard_api` package. GREEN exited 0 with 14 health, production-settings, exact-origin, RFC error, validation redaction, internal redaction, CSRF, proxy/origin/host, body-limit, request-ID, and documentation contract tests. Service Ruff and diff checks exited 0. Editable installation succeeded with declared dependencies; isolated sdist and wheel builds succeeded after adding package documentation. The only warning is the already recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/{pyproject.toml,README.md,.gitignore}`, `services/control-api/src/loopguard_api/{__init__.py,settings.py,errors.py,app.py}`, `services/control-api/tests/{test_health.py,test_error_contract.py}`, and `docs/reference/control-api-errors.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `CLOUD-T01` as `feat: scaffold hosted control api`; verify the installed-package health/error tests; then begin `CLOUD-T02` with failing tenant-context, direct-query isolation, distinct-sequence, and idempotent-ingest tests.
