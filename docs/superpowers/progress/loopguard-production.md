# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T03`
- Last completed step: `CLOUD-T03` completed inline. Bearer authentication now validates an allowlisted asymmetric JWT algorithm, exact key ID, cached OIDC discovery/JWKS, signature, issuer, audience, expiry, issued time, subject, tenant UUID, and optional flow nonce before querying membership. The database membership store enters a tenant-scoped transaction, rejects unknown roles, and constructs an immutable principal with explicit permissions. `/v1/me` exposes only safe identity metadata and action authorization fails closed for viewers; tenant identity is never accepted from a request body.
- Expected/observed last commit: Expected task commit `feat: secure control api with oidc roles`; observed prior task commit `9594b34 feat: add tenant-scoped control plane schema`.
- Task dependencies and owned files: `CLOUD-T03` establishes the authenticated principal/permission dependencies used by all protected routers. `CLOUD-T04` will add one-use host pairing, repository-bound signed hook ingest, and an outbound daemon relay with durable acknowledgement checkpoints.
- Red/green/regression commands with exit codes and counts: T03 RED exited 2 on the absent authentication module. GREEN focused auth tests exited 0 with 11 cases covering wrong issuer/audience, expired or malformed claims, unknown signing key, nonce mismatch, valid identity, absent membership, and viewer/operator permissions. The complete service gate exited 0 with 31 passed and one Docker-only PostgreSQL RLS skip; Ruff and Alembic drift checks exited 0. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/src/loopguard_api/{auth.py,authorization.py,app.py,settings.py}`, `services/control-api/tests/test_auth.py`, and the declared `PyJWT[crypto]` runtime dependency.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T03` as `feat: secure control api with oidc roles`; then begin `CLOUD-T04` with failing one-use pairing, repository substitution, hook replay, relay reconnect, duplicate batch, and backpressure tests.
