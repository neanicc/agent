# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T06`
- Last completed step: `ROUTING-T06` completed inline. Holdout assignment is stable, HMAC-keyed, bounded, and callable only after explicit opt-in; shadow decisions record the proposal while preserving the active model and effort. Reports compare verified success, Wilson 95% intervals, regression rate, complete observed cost, wall time, and interventions for control versus routed groups, require minimum samples before any recommendation, and keep estimated counterfactual savings explicitly null. CLI commands validate strict policies and evaluate bounded time windows without invoking a model.
- Expected/observed last commit: Expected task commit `feat: evaluate routing in shadow and holdout modes`; observed predecessor `b661187 feat: record routing cost and quality outcomes`.
- Task dependencies and owned files: `ROUTING-T06` evaluates only T05 observed outcomes and registers optional evaluator/recorder/assigner services without activating routing. `ROUTING-T07` next owns phase persistence, recovery, managed adapter switching, and the remaining daemon service registry; attached or shadow surfaces remain unchanged.
- Red/green/regression commands with exit codes and counts: T06 RED exited 2 during collection with the expected missing `loopguard.router.evaluation` module. GREEN exited 0 with 74 router tests. The routing/CLI/daemon regression first passed router/CLI behavior but hit the expected Unix-socket sandbox timeout; the permitted rerun exited 0 with 93 tests. Ruff, generated CLI docs, policy validation (`deterministic-v1`, 7 rules), empty evaluation JSON, and diff whitespace checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{evaluation.py,outcomes.py,policy.py,__init__.py}`, `loopguard/src/loopguard/{cli.py,cli_docs.py}`, `loopguard/src/loopguard/control/daemon.py`, `loopguard/tests/router/test_evaluation.py`, and generated `loopguard/docs/reference/cli.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T06 router suite 74 passed and routing/CLI/daemon regression 93 passed; T05 focused 10 passed; final full Python suite after T04 713 passed; T03 isolated wheel/package-data verification passed; Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T06` as `feat: evaluate routing in shadow and holdout modes`, verify the clean tree and commit subject, then read all of `ROUTING-T07` and begin with failing managed phase-transition/recovery tests.
