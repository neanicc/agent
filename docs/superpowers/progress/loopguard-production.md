# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T01`
- Last completed step: `ROUTING-T01` completed inline. The provider-neutral catalog is immutable and deterministically ordered; filters by surface, effort, capabilities, context, evidence lifetime, and exact `Decimal` price limits; never treats unknown price as free; records source/revision/capability provenance; canonicalizes signed payloads; rejects untrusted, premature, or expired updates; and retains a last-known-valid snapshot only with explicit stale status and reason.
- Expected/observed last commit: Expected task commit `feat: add provider-neutral model catalog`; observed predecessor `9a3de00 docs: record managed integrations milestone`.
- Task dependencies and owned files: `ROUTING-T01` depends on the managed adapter capability surfaces completed in tranche 5 and owns only the new router catalog package/tests. `ROUTING-T02` next adds bounded, zero-token task and trajectory feature extraction using repository metadata and existing verification events.
- Red/green/regression commands with exit codes and counts: T01 RED exited 2 during collection with the expected missing `loopguard.router` package. GREEN exited 0 with 16 catalog tests; routing/adapter/control regression exited 0 with 417 tests. Task-owned Ruff lint and formatting, plus diff whitespace validation, exited 0. Tranche 5 completion evidence remains 653 Python tests, 3 Node tests, strict TypeScript, production npm audit with 0 vulnerabilities, generated CLI docs, package/template checks, and stable live read-only agent probes.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{__init__.py,catalog.py}` and `loopguard/tests/router/{conftest.py,test_catalog.py}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T01 focused 16 passed and routing/adapter/control regression 417 passed; preceding full Python suite 653 passed, Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T01` as `feat: add provider-neutral model catalog`, verify the clean tree and commit subject, then read all of `ROUTING-T02` and begin with failing deterministic feature-table tests.
