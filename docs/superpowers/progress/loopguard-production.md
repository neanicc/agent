# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T02`
- Last completed step: `ROUTING-T02` completed inline. A zero-token extractor now deterministically classifies search/edit/debug/migration/review/UI/repair work from a bounded prompt, precomputed repository snapshot, and bounded trajectory. It records repository scale, languages, changed symbols, dependency depth, test scope, tool requirements, verification failures, retries, stable loop fingerprints, and contextual sensitivity markers; hard caps all inputs/outputs; avoids token-budget and generic-tool-error false positives; and never crawls the repository or invokes a provider.
- Expected/observed last commit: Expected task commit `feat: extract zero-token routing features`; observed predecessor `04a1966 feat: add provider-neutral model catalog`.
- Task dependencies and owned files: `ROUTING-T02` consumes only precomputed repository/trajectory evidence and produces immutable `TaskProfile` values for the versioned rules in `ROUTING-T03`. It does not select models or introduce a hidden inference call.
- Red/green/regression commands with exit codes and counts: T02 RED exited 2 during collection with the expected missing `loopguard.router.features` module. GREEN exited 0 with 10 feature-table/bounds/false-positive tests; routing plus verification regression exited 0 with 130 tests. Task-owned Ruff lint, formatting, and diff whitespace validation exited 0. T01 remains green at 16 focused tests and its prior routing/adapter/control gate at 417 tests.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{__init__.py,features.py}` and `loopguard/tests/router/{conftest.py,test_features.py}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T02 focused 10 passed and routing/verification regression 130 passed; T01 focused 16 passed and routing/adapter/control regression 417 passed; preceding full Python suite 653 passed, Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T02` as `feat: extract zero-token routing features`, verify the clean tree and commit subject, then read all of `ROUTING-T03` and begin with the failing routing decision matrix and fallback tests.
