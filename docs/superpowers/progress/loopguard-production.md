# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 4 of 13 complete — tranche 5 managed integrations is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-agent-integrations.md`, `INTEGRATIONS-T05`
- Last completed step: `INTEGRATIONS-T05` completed inline. Managed Codex now uses the stable App Server JSON-RPC lifecycle with installed-CLI-generated compatibility evidence, explicit non-experimental client identity, paginated model and reasoning-effort discovery, exact active-turn steering and interruption, distinct between-turn Responses API injection, typed events and approval requests, bounded queues/frames/timeouts, redacted diagnostics, process-tree termination, fail-closed proof/baseline/worktree-lease authorization, owner-only durable thread/turn state, and restart recovery without duplicate turns.
- Expected/observed last commit: Expected task commit subject `feat: manage codex through app server`; observed predecessor `a8a770c feat: isolate concurrent managed agents in worktrees`.
- Task dependencies and owned files: `INTEGRATIONS-T05` consumes the managed-run safety request from `adapters/base.py`, the verification proof/baseline contract, and the context worktree lease boundary. It owns the bounded JSON-RPC transport, Codex managed adapter, pinned App Server fixture, and their protocol tests. `INTEGRATIONS-T06` next adds the separate managed Claude SDK bridge without weakening or sharing vendor lifecycle semantics.
- Red/green/regression commands with exit codes and counts: T05 RED exited 2 during collection because `loopguard.adapters.codex_managed` did not exist. The final focused Codex lifecycle/protocol gate exited 0 with 19 passed. The related adapter/context/verification gate exited 0 with 94 passed after rerunning outside the sandbox for its local Unix socket. Full Ruff and diff whitespace checks exited 0, and the final full Python regression exited 0 with 600 passed. A non-billing live smoke against installed `codex-cli 0.144.0` passed initialize, one-item model discovery, and exact provider-capability discovery after rerunning with access to its local Codex state. Repository-wide Ruff format remains a known pre-existing non-gate across 66 unrelated files; all T05 files pass focused format checking. Only the recorded external Starlette deprecation remains.
- Evidence/artifact paths: `loopguard/src/loopguard/adapters/{jsonrpc.py,codex_managed.py}`, `loopguard/tests/adapters/test_codex_managed.py`, and `loopguard/tests/fixtures/codex_app_server.jsonl`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate.
- Last successful cumulative verification: 2026-07-15 — full Python suite 600 passed, managed integration related gate 94 passed, full Ruff clean, diff whitespace clean, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports 66 pre-existing unrelated files and is tracked as a formatting baseline, not silently rewritten during T05.
- Active blockers: None.
- Next exact action: Commit `INTEGRATIONS-T05` as `feat: manage codex through app server`, verify the clean tree and commit subject, then read and begin `INTEGRATIONS-T06` with failing managed Claude SDK bridge lifecycle and capability tests.
