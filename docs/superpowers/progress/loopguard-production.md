# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 10 of 13 complete — hosted cloud control plane complete; control-surface release-quality tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T09`
- Last completed step: `SURFACE-T09` completed inline. The web console now provides tenant-scoped policy, cost, device, and immutable audit administration. Policy changes enforce managed minimums in both browser and service, expose source and precedence, and preview affected capabilities. Costs separate observed agent, judge, verification, critic, and repair categories while identifying unavailable counterfactual estimates honestly. Device management shows last-seen/key metadata and requires explicit scoped confirmation before idempotent revocation. Audit records are read-only, filterable, and exportable with spreadsheet-formula injection protection.
- Expected/observed last commit: Expected task commit `feat: add policy cost device and audit console`; observed prior task commit `df4f594 feat: add secure ios approvals and notifications`.
- Task dependencies and owned files: `SURFACE-T09` consumes the T01 generated control API client and Cloud T09 tenant-aware administration endpoints. It strengthens the service response contracts and policy/device semantics, regenerates checked-in OpenAPI/TypeScript bindings, adds four console routes and navigation entries, and adds tenant-boundary browser fixtures without exposing cross-tenant identifiers or editable audit state.
- Red/green/regression commands with exit codes and counts: Managed-policy component tests first failed because the editor did not exist. Final web verification passed 8 Vitest files/32 tests, strict TypeScript, zero-warning ESLint, and a production Next.js build. The focused Chromium tenant-boundary suite passed all 3 tests. The control API passed 71 tests with one intentional integration skip and the recorded external Starlette warning; Ruff lint and changed-file format checks passed. `git diff --check` passed.
- Evidence/artifact paths: `apps/web/src/components/policy-editor.tsx`, `apps/web/src/components/admin-pages.tsx`, `apps/web/src/app/(console)/policies/`, `apps/web/src/app/(console)/costs/`, `apps/web/src/app/(console)/devices/`, `apps/web/src/app/(console)/audit/`, `apps/web/e2e/tenant-boundaries.spec.ts`, `services/control-api/src/loopguard_api/routes/`, and `contracts/control-api.openapi.json`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T09` as `feat: add policy cost device and audit console`; then begin `SURFACE-T10` with failing axe, keyboard/landmark/live-region, source-design, visual-state, and native accessibility tests.
