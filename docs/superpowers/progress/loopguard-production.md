# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T05`
- Last completed step: `ROUTING-T05` completed inline. Durable routing outcomes now preserve selected versus actual model, effort/phase/rule, automatic status, nullable token usage, provider-reported and estimated agent costs as separate exact values, reconciled component costs, nullable verification, regressions, interruptions, repairs, and wall time. `total_cost` is available only when every required component is explicitly known; `known_total` and `cost_status` expose partial knowledge without converting missing data to zero. No avoided-cost claim exists.
- Expected/observed last commit: Expected task commit `feat: record routing cost and quality outcomes`; observed predecessor `0892f8e feat: enforce optional service cost budgets`.
- Task dependencies and owned files: `ROUTING-T05` records T03 decisions and later provider/verification facts without estimating inside the store. It owns a versioned owner-only SQLite outcome store with stable bounded reads, duplicate protection, strict schema validation, symlink refusal, corruption handling, and exact Decimal JSON round trips. `ROUTING-T06` next adds opt-in stable shadow/holdout evaluation over these observed outcomes.
- Red/green/regression commands with exit codes and counts: T05 RED exited 2 during collection with the expected missing `loopguard.router.outcomes` module. GREEN exited 0 with 10 accounting/store tests; the full router regression exited 0 with 69 tests. Router Ruff lint, formatting, and diff whitespace validation exited 0. The preceding final full regression remains 713 passed with only the recorded external Starlette warning.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{outcomes.py,store.py,__init__.py}` and `loopguard/tests/router/test_outcomes.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T05 focused 10 and full router regression 69 passed; final full Python suite after T04 713 passed; T04 focused 41 and routing/context/guard regression 136 passed; T03 isolated wheel/package-data verification passed; Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T05` as `feat: record routing cost and quality outcomes`, verify the clean tree and commit subject, then read all of `ROUTING-T06` and begin with failing stable holdout-assignment and shadow-mode tests.
