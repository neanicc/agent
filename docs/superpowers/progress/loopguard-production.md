# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-preference-engine.md`, `PREFERENCES-T05`
- Last completed step: `PREFERENCES-T05` completed inline. The optional critic runs only for classified UI work with a current redaction-confirmed screenshot, reference or explicit visual rule, positive reservation ID/amount, and compatible local-only policy. PNG/JPEG evidence is dimension-bounded, format/hash-checked, cropped before upload, re-encoded without metadata, and paired with redacted OCR/filenames/rule/metadata inside a fixed hostile-data envelope; the model receives no tools. Strict JSON is converted to cited preference verdicts, learned severity is always `inform`, all other severity is capped at configured policy, and model/prompt/cost/reservation/input hashes are recorded. Malformed output, identity mismatch, provider failure, or reported overspend is inconclusive rather than authoritative.
- Expected/observed last commit: Expected task commit `feat: add budgeted visual preference critic`; observed predecessor `aa3a4e3 feat: learn reviewable soft preferences`.
- Task dependencies and owned files: `PREFERENCES-T05` consumes T01 verdicts, the T02 Pillow extra, and an upstream trusted budget reservation. It remains optional and zero-call when ineligible. `PREFERENCES-T06` next owns durable profile snapshots, cache invalidation, preference evidence links, hard/soft verification semantics, overrides, daemon registration, and digest warnings.
- Red/green/regression commands with exit codes and counts: T05 RED exited 2 during collection with the expected missing critic module. GREEN and full preference regression exited 0 with 42 tests, including real Pillow crop dimensions, byte-level PNG metadata stripping, hostile rule/source/terminal/OCR/filename fixtures, credential redaction, no-tool payloads, reservation/local-only/redaction eligibility, strict schema, cost overrun, severity caps, and explicit no-reference rules. Ruff and diff whitespace checks exited 0. Pillow 12.3 was installed from the declared `preferences` extra to execute rather than skip crop QA.
- Evidence/artifact paths: `loopguard/src/loopguard/preferences/{visual_critic.py,models.py,__init__.py}` and `loopguard/tests/preferences/test_visual_critic.py`, plus the prior preference suite.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `PREFERENCES-T05` as `feat: add budgeted visual preference critic`, then begin `PREFERENCES-T06` with failing hard/soft verification integration and profile-version restart tests.
