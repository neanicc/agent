# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T08`
- Last completed step: `CLOUD-T08` completed inline. Notification, action-expiry, artifact-deletion, verification, and repair workflows derive stable workflow IDs and activity idempotency keys from opaque domain IDs, use bounded retry, deduplicate completed work, and reject secret-like history inputs. Audit stores capture actor, action, target, request, before/after hashes, result, device/IP metadata, and time. Ordinary updates/deletes fail in the domain store and SQLAlchemy before flush; PostgreSQL additionally revokes public mutation and installs a rejecting trigger. The explicit retention matrix covers every required data class, honors legal holds, limits de-identification to a privileged human-authorized path, and appends a tombstone rather than silently erasing history.
- Expected/observed last commit: Expected task commit `feat: add durable workflows and immutable audit`; observed prior task commit `2855f5e feat: store encrypted evidence artifacts`.
- Task dependencies and owned files: `CLOUD-T08` establishes durable orchestration and immutable accountability. `CLOUD-T09` will add the remaining versioned client/admin schemas, query routes, device proof-of-possession pairing, effective capability computation, migration-backed preferences/usage, and OpenAPI contract coverage.
- Red/green/regression commands with exit codes and counts: T08 RED exited 2 on absent workflow/notification/audit modules. GREEN focused workflow/audit tests passed seven, including transient notification retry without duplicate acceptance, deterministic replay, child cancellation, in-memory and ORM immutability, retention coverage, and audited tenant de-identification. The complete service gate exited 0 with 56 passed and one Docker-only PostgreSQL RLS skip; Ruff and Alembic drift checks passed. SQLite downgrade/upgrade and PostgreSQL offline compilation both exited 0 and emitted the forced-RLS plus append-only trigger DDL. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/src/loopguard_api/{workflows.py,notifications.py,audit.py,db.py}`, `services/control-api/tests/{test_workflows.py,test_audit.py}`, `services/control-api/alembic/versions/0001_initial.py`, and `docs/reference/cloud-retention.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T08` as `feat: add durable workflows and immutable audit`; then begin `CLOUD-T09` with failing managed-preference, observed-cost, tenant-safe devices/audit, one-use device-pairing, stale-capability, and complete OpenAPI matrix tests.
