# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 7 of 13 complete — tranche 8 browser acceleration is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-browser-acceleration.md`, `BROWSER-T02`
- Last completed step: `BROWSER-T02` completed inline. The broker now pins Playwright 1.61.1, reuses at most one live process per browser, and creates one fresh context plus random capability/artifact directory per session. Contexts default to fixed locale/timezone and blocked service workers, and close on explicit session stop, disconnect grace, idle TTL, process crash, or broker shutdown. Every request is session-bound. Navigation is exact-origin allowlisted, re-resolves DNS on every request/redirect, and denies non-HTTP schemes, embedded credentials, localhost, metadata, and every non-public IPv4/IPv6 range unless trusted private-network policy is explicit. Uploads, downloads, and screenshots accept safe artifact names only and remain inside the session directory. Unix sockets require owner-only real parents, refuse occupied/symlink paths, are chmod 0600, and are unlinked only if inode identity still matches; all methods require constant-time daemon-capability authentication.
- Expected/observed last commit: Expected task commit `feat: reuse browsers with isolated contexts`; observed predecessor `bb7b636 feat: define isolated browser broker protocol`.
- Task dependencies and owned files: `BROWSER-T02` consumes the T01 frame/schema contract and extends page actions only for confined upload/download operations. T03 will supervise this pinned server from Python and register session lifecycle leases in the daemon.
- Red/green/regression commands with exit codes and counts: T02 RED exited 1 with missing `broker.js`. The first implementation run exposed and fixed macOS `/var` to `/private/var` canonical alias handling while retaining leaf symlink checks. Final broker tests exited 0 with 16 lifecycle/protocol/security cases, strict TypeScript exited 0, and the production dependency audit reports 0 vulnerabilities. Tests cover storage/context isolation, process reuse, all cleanup paths, crash relaunch, random capability ownership, safe artifacts, dangerous origin/address denial, DNS rebinding, disconnect isolation, owner-only sockets, and wrong/correct daemon capabilities.
- Evidence/artifact paths: `loopguard/integrations/browser-broker/src/{broker.ts,server.ts,transport.ts,broker.test.ts,security.test.ts,testing.ts,protocol.ts,protocol.test.ts}` and the package lock/manifest.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `BROWSER-T02` as `feat: reuse browsers with isolated contexts`, then begin `BROWSER-T03` with failing Python supervision restart/timeout tests and daemon lifecycle registration.
