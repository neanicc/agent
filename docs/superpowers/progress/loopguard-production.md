# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 8 of 13 complete — tranche 9 cloud control plane is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-cloud-control-plane.md`, `CLOUD-T02`
- Last completed step: `CLOUD-T02` completed inline. The control service now owns explicit SQLAlchemy models and an Alembic baseline for tenants, principals, hosts, repositories, sessions, events, actions, artifacts, repair state, signing state, audit records, and a transactional relay outbox. Tenant context is mandatory, automatically scopes ORM and Core reads and writes, rejects cross-tenant and tenant-less mutations, and is reinforced by forced PostgreSQL row-level-security policies. Event ingest is idempotent, locks each session before assigning its monotonic sequence, obtains its cloud-global cursor from a PostgreSQL sequence, preserves local/repository/client cursor domains independently, and writes the relay outbox atomically.
- Expected/observed last commit: Expected task commit `feat: add tenant-scoped control plane schema`; observed prior task commit `4b6f0f5 feat: scaffold hosted control api`.
- Task dependencies and owned files: `CLOUD-T02` establishes the durable tenant and cursor boundary required by every remaining cloud route. `CLOUD-T03` will authenticate OIDC access tokens through cached JWKS and derive permissions from persisted tenant membership rather than request bodies.
- Red/green/regression commands with exit codes and counts: T02 focused RED tests failed for the intentionally missing models/repositories. GREEN exited 0 with 20 service tests passing and one conditional PostgreSQL RLS integration test skipped because Docker is not installed locally. Repository, aggregate, raw Core, direct-write, bulk-write, event-idempotency, outbox-atomicity, and cursor-domain cases all pass. Ruff exited 0 and `alembic check` reported no upgrade operations. The migration completed SQLite upgrade/downgrade/upgrade and PostgreSQL offline compilation; the opt-in Testcontainers test applies it under a non-superuser role in Docker-capable CI. The only warning is the recorded external Starlette TestClient deprecation.
- Evidence/artifact paths: `services/control-api/{alembic.ini,alembic/,src/loopguard_api/db.py,src/loopguard_api/models.py,tests/test_tenant_isolation.py,tests/test_postgres_rls.py}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `CLOUD-T02` as `feat: add tenant-scoped control plane schema`; then begin `CLOUD-T03` with failing issuer, audience, algorithm, expiry, membership, and role-permission tests.
