# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T08`
- Last completed step: `SURFACE-T08` completed inline. The iOS client now fetches authenticated action detail before enabling controls, renders immutable target/effect/risk/parameter-hash/expected-state/countdown facts, signs the exact canonical challenge with the paired device key, requires device-owner authentication for server-designated high risk, and distinguishes reviewed, signed, queued, delivered, host-executing, reconciling, and terminal receipt states. Expiry, double submit, client/server device revocation, host offline, acknowledgement loss, stale content, and deep-linked expiry fail closed without automatic re-sign/resubmit. A real WidgetKit extension shares non-sensitive ActivityAttributes, has safe previews/deep links, and ends at a bounded lifetime. APNs registration is paired-device and tenant scoped; push payloads expose only generic text and opaque IDs.
- Expected/observed last commit: Expected task commit `feat: add secure ios approvals and notifications`; observed prior task commit `a995a1e feat: add ios safety inbox and run timeline`.
- Task dependencies and owned files: `SURFACE-T08` consumes the T06 paired device proof, T07 native safety inbox, cloud signed-action lifecycle, and notification workflow boundary. It adds typed authenticated action-read fields and paired-device push registration to the versioned control API, regenerates the checked-in OpenAPI/TypeScript bindings, and publishes the native action, notification, and Live Activity implementation. It does not activate repair navigation.
- Red/green/regression commands with exit codes and counts: Action expiry/duplicate/signature tests first failed because the action types were absent; authenticated review fields, push registration, notification privacy, and automatic expiry tests each failed before implementation. Final XcodeBuildMCP signed Simulator run passed all 41 iOS unit/UI tests under Swift 6 strict concurrency and warnings-as-errors. The focused control API gate passed 71 tests with one intentional integration skip and the recorded external Starlette warning; Ruff was clean. Generated web bindings passed strict TypeScript and 5 API-client tests. `simctl push` successfully delivered the local generic action fixture. Plists/entitlements and `git diff --check` passed.
- Evidence/artifact paths: `apps/ios/LoopGuard/Features/Actions/`, `apps/ios/LoopGuard/Notifications/`, `apps/ios/LoopGuardShared/Activities/`, `apps/ios/LoopGuardWidgets/`, `apps/ios/LoopGuardUITests/ActionReviewUITests.swift`, `services/control-api/src/loopguard_api/routes/actions.py`, `services/control-api/src/loopguard_api/routes/devices.py`, `contracts/control-api.openapi.json`, `/private/tmp/loopguard-action.apns`, and `/private/tmp/loopguard-t08-final-attachments/218866DF-BCE4-49D0-A3F4-C915E9FDF8FA.png`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T08` as `feat: add secure ios approvals and notifications`; then begin `SURFACE-T09` with failing managed-policy validation and tenant-boundary tests for policy, cost, device, and immutable audit surfaces.
