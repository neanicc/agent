# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 7 of 13 complete — tranche 8 browser acceleration is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-browser-acceleration.md`, `BROWSER-T06`
- Last completed step: `BROWSER-T06` completed inline. Phase-aware execution now emits validated argv arrays: impacted iteration uses the primary project with traces off until first retry, completion uses every required project with retain-on-failure traces, and PR runs the full suite with validated optional sharding. The Playwright impact plugin contributes offline coverage edges and conservatively expands fallback candidates. The pinned opt-in `@loopguard/playwright` adapter authenticates to the local broker, leases a supported Playwright browser-server endpoint, preserves standard fixtures/reporters/traces, closes per-test contexts, releases leases after failures, and falls back to native Playwright unless broker use is required. Doctor reports config and fixture activation without traversing dependency/build directories. Broker endpoint capabilities are random, session-bound, idle/disconnect cleaned, and never persisted.
- Expected/observed last commit: Expected task commit `feat: accelerate browser verification by phase`; observed predecessor `4bedfea feat: select impacted playwright tests`.
- Task dependencies and owned files: `BROWSER-T06` extends the T01-T05 broker/client/manifest contracts and keeps ordinary Playwright Test on the public browser-server connection API; the standalone `page.run` probe is not used as acceleration evidence. T07 will add paired speed/safety histograms, escape counters, benchmark policy, and operator documentation.
- Red/green/regression commands with exit codes and counts: T06 Python RED exited 2 with the expected missing execution module. GREEN/full focused gate exited 0 with 56 browser, impact, and doctor tests. Clean broker install passed 21 Node tests and strict typecheck; clean adapter install passed 4 lifecycle/fallback unit tests plus one real Chromium integration that ran native, accelerated twice, intentional failure cleanup, trace/report validation, endpoint reuse, and broker-unavailable fallback. Both production npm audits reported zero vulnerabilities; Ruff, CLI doctor compatibility, strict TypeScript, and diff checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/browser/{execution.py,client.py,__init__.py}`, `loopguard/src/loopguard/verify/plugins.py`, `loopguard/src/loopguard/control/diagnostics.py`, `loopguard/tests/browser/{test_execution.py,test_client.py}`, `loopguard/integrations/browser-broker/src`, and `loopguard/integrations/playwright-loopguard`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `BROWSER-T06` as `feat: accelerate browser verification by phase`, then begin `BROWSER-T07` with failing cold/warm/impacted/full metric-separation and selection-escape tests.
