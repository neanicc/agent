# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T02`
- Last completed step: `SURFACE-T02` completed inline. A strict Next.js/React web foundation now generates checked TypeScript definitions from the versioned OpenAPI snapshot, separates server bearer authentication from the same-origin browser BFF, maps RFC problem documents to typed errors, forwards bounded request IDs/abort signals, applies CSRF to state changes, and never exposes authorization headers to browser code. The locked production design system defines semantic light/dark cobalt tokens, Space Grotesk/IBM Plex type roles, row-led Workbench geometry, status/content grammar, accessibility, responsive behavior, and the narrow native-only Liquid Glass boundary.
- Expected/observed last commit: Expected task commit `feat: scaffold typed web control console`; observed prior task commit `d74f481 feat: publish versioned control api contract`.
- Task dependencies and owned files: `SURFACE-T02` supplies the authenticated BFF, client wrappers, generated contract, token system, and product design contract consumed by every later surface task. `SURFACE-T03` owns the capability-derived console shell, core route inventory, async states, and replay-safe timeline; Repairs must remain absent.
- Red/green/regression commands with exit codes and counts: T02 RED failed at module resolution because the browser/server clients were absent. GREEN passed four client isolation/error tests. A clean `npm ci` applied the exact-version, fail-closed js-yaml 5 compatibility patch for the OpenAPI generator; full npm audit reported zero vulnerabilities; regeneration passed; all four unit tests passed; strict typecheck, ESLint, and the Next production build all exited 0. The initial newest-registry TypeScript/ESLint pins were corrected to their newest compatible peer lines rather than bypassing validation.
- Evidence/artifact paths: `apps/web/src/lib/api/`, `apps/web/src/app/api/control/[...path]/route.ts`, `apps/web/src/styles/`, `apps/web/package-lock.json`, and `docs/product/design-system.md`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T02` as `feat: scaffold typed web control console`; then begin `SURFACE-T03` with failing timeline replay/gap tests before adding visible routes.
