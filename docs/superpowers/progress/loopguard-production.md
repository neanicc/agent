# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-preference-engine.md`, `PREFERENCES-T02`
- Last completed step: `PREFERENCES-T02` completed inline. The compiler deterministically layers packaged safety defaults, organization strengthening, bounded repository TOML/design tokens/instruction sections, authenticated explicit user rules, deletions, and learned soft rules. Safety cannot be weakened or deleted; untrusted repository `block` requests are clamped to warnings and instruction text stays inert data. Every source has a stable content hash, token documents are bounded and schema-checked, YAML aliases/anchors and unsupported keys/formats fail closed, and file reads reject escapes, symlinks, replacement races, duplicates, and oversized inputs.
- Expected/observed last commit: Expected task commit `feat: compile repository and user preferences`; observed predecessor `e387632 feat: define editable preference policy`.
- Task dependencies and owned files: `PREFERENCES-T02` consumes T01 contracts and packages `defaults.toml` in wheels. It adds the `preferences` dependency group for bounded JSON Schema, YAML, and image metadata support and keeps `all-dev` complete. `PREFERENCES-T03` next owns offline deterministic artifact evaluators.
- Red/green/regression commands with exit codes and counts: T02 RED exited 2 during collection with the expected missing compiler. GREEN and regression exited 0 with 16 preference/metadata tests. Ruff and diff whitespace checks exited 0. The first isolated wheel build hit expected sandbox DNS denial and the no-isolation fallback exposed the local setuptools 65 baseline; the permitted isolated build then succeeded with setuptools 83/wheel 0.47 and confirmed compiler, models, and packaged defaults in the wheel.
- Evidence/artifact paths: `loopguard/src/loopguard/preferences/{compiler.py,defaults.toml,models.py,__init__.py}`, `loopguard/tests/preferences/{conftest.py,test_compiler.py,test_models.py}`, `loopguard/tests/fixtures/preferences/repo`, `loopguard/pyproject.toml`, and `loopguard/tests/control/test_error_contract.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `PREFERENCES-T02` as `feat: compile repository and user preferences`, then begin `PREFERENCES-T03` with failing token/accessibility artifact tests.
