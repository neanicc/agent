# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T02`
- Last completed step: `HEAL-T02` completed inline. Duck-typed Airflow, OpenLineage, GitHub Actions, and generic webhook adapters now reduce allowlisted bounded fields to one `FailureEvent` contract. Stable fingerprints exclude volatile run/check IDs while binding repository, revision family, pipeline/step, exception type, repository-relative top frames, and canonical schema. Signed cloud intake verifies the raw body, credential key, repository binding, `repair:intake` scope, timestamp/skew, one-use nonce, expiry, revocation/rotation, and body/decompression bounds before parsing; source labels remain untrusted data. Active duplicates are atomically collapsed by tenant, repository, and fingerprint.
- Expected/observed last commit: Expected task commit `feat: normalize pipeline failure intake`; observed prior task commit `4fedcf1 feat: define gated pipeline repair workflow`.
- Task dependencies and owned files: `HEAL-T02` consumes T01 contracts and the cloud-owned repository hook lifecycle. It adds dependency-light source adapters, normalization/fingerprinting, scoped credential rotation, the signed `/v1/repair-intake` boundary, deterministic contract/reference/client regeneration, and a hosted fail-closed requirement for durable hook/intake adapters. It does not import Airflow, OpenLineage, or GitHub orchestrator runtimes.
- Red/green/regression commands with exit codes and counts: Library tests first failed because intake was absent and cloud tests because hook credentials had no repair scope. Final normalization/model/legacy public-API coverage passes 21 tests; the entire control API passes 78 tests with one intentional Docker/PostgreSQL skip and the recorded external Starlette warning; strict Ruff passes both packages. The web contract consumer passes all 34 unit tests, strict TypeScript, and zero-warning ESLint. Cross-source fixtures share fingerprints, volatile GitHub IDs do not affect them, malformed/external stack frames fail closed, and cloud tests cover replay, repository substitution, missing scope, stale/rotated/revoked credentials, gzip bombs, forged source shapes, provider-header substitution, deduplication, and hostile prompt/SSRF text. Generated OpenAPI and human reference determinism pass.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/intake.py`, `loopguard/src/loopguard/heal/fingerprint.py`, `loopguard/src/loopguard/heal/integrations/`, `loopguard/tests/fixtures/heal/events/`, `services/control-api/src/loopguard_api/routes/repair_intake.py`, `services/control-api/tests/test_repair_intake.py`, `contracts/control-api.openapi.json`, `docs/reference/control-api.md`, and `apps/web/src/lib/api/generated.ts`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T02` as `feat: normalize pipeline failure intake`; then begin `HEAL-T03` with type-preserving redaction and schema-delta tests.
