# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 7 of 13 complete — tranche 8 browser acceleration is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-browser-acceleration.md`, `BROWSER-T05`
- Last completed step: `BROWSER-T05` completed inline. A strict versioned manifest now records discovered test files, projects, tags, setup dependencies, route/component coverage, and imports plus configured smoke/shared/config/ignored paths. The offline selector combines direct test/route/component/import edges with an optional resolved context dependency graph, recursively includes setup dependencies in topological order, and emits deterministic per-test explanations. Unknown source, shared infrastructure, and Playwright configuration changes always add the smoke fallback; documented ignored paths may safely select zero. The TypeScript generator invokes Playwright's supported `test --list --reporter=json` CLI with bounded output and no shell, then merges strict `.loopguard/browser.toml` mappings and atomically emits the Python-consumable manifest.
- Expected/observed last commit: Expected task commit `feat: select impacted playwright tests`; observed predecessor `04dc6b2 feat: reuse scoped browser authentication safely`.
- Task dependencies and owned files: `BROWSER-T05` consumes the existing context import-graph contract through explicit resolved dependency edges and does not invoke a model. T06 will convert selections into phase-specific argv plans and ship the opt-in Playwright fixture/config package without weakening completion/PR gates.
- Red/green/regression commands with exit codes and counts: T05 Python RED exited 2 with the expected missing manifest module. GREEN exited 0 with 6 Python tests covering routes plus setup, shared/config/unknown fallback, component/direct-test selection, dependency graph edges, docs ignores, duplicate/unsafe manifest rejection, and deterministic explanations. The standalone TypeScript generator exited 0 with 2 discovery/config/unsafe-mapping tests and strict ESNext typecheck. Ruff and diff checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/browser/{manifest.py,selection.py,__init__.py}`, `loopguard/tests/browser/test_selection.py`, and `loopguard/scripts/{playwright_manifest.ts,playwright_manifest.test.ts}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `BROWSER-T05` as `feat: select impacted playwright tests`, then begin `BROWSER-T06` with failing phase/trace argv-policy and real fixture fallback/isolation tests.
