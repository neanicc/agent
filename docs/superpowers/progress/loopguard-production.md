# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T04`
- Last completed step: `ROUTING-T04` completed inline. Optional judge/context/visual services now use atomic exact-`Decimal` allocations beneath the hard session ceiling; denied or failed reservations retain the typed detector decision without calling the provider. Judge outcomes use cross-run, privacy-redacted incident identity with policy/model/prompt invalidation, TTL/LRU bounds, single-flight concurrency, validated-outcome-only caching, fail-safe retry semantics, explicit reset, and optional AES-GCM owner-only atomic persistence. Context digests independently enforce character and token estimates. Strict judge parsing prevents coercible malformed JSON from becoming cached truth.
- Expected/observed last commit: Expected task commit `feat: enforce optional service cost budgets`; observed predecessor `f4ffbf5 feat: route managed phases with deterministic policy`.
- Task dependencies and owned files: `ROUTING-T04` preserves the existing hard total-spend detector and replaces only the old run/detector judge memoization. A transient active-incident alias preserves one decision across a continuously firing run while the reusable cache key excludes run ID and includes normalized behavior/context. `ROUTING-T05` next records actual route/model/usage/quality outcomes with unknown accounting kept distinct from zero.
- Red/green/regression commands with exit codes and counts: T04 RED exited 2 during collection with the expected missing `loopguard.router.budgets` module. The final focused budget/cache/context/guard/judge gate exited 0 with 41 tests; the routing/context/guard regression exited 0 with 136 tests. The final full regression exited 0 with 713 tests and only the recorded external Starlette warning. Full Ruff, task-owned formatting, generated CLI docs, and diff whitespace validation exited 0. The full gate also exposed and fixed a pre-existing 1-in-256 ciphertext-tamper test flake by deterministically flipping a bit.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{budgets.py,judge_cache.py}`, `loopguard/src/loopguard/{config.py,guard.py,judge.py}`, `loopguard/src/loopguard/context/digest.py`, `loopguard/tests/router/{__init__.py,test_budgets.py,test_judge_cache.py}`, `loopguard/tests/context/test_digest.py`, and `loopguard/tests/verify/test_artifacts.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — final full Python suite 713 passed; T04 focused 41 and routing/context/guard regression 136 passed; T03 focused 14, routing/adapter/control regression 442, and isolated wheel/package-data verification passed; Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T04` as `feat: enforce optional service cost budgets`, verify the clean tree and commit subject, then read all of `ROUTING-T05` and begin with failing reconciled outcome-accounting tests.
