# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T01`
- Last completed step: `HEAL-T01` completed inline. Pipeline repairs now use strict immutable evidence contracts and an explicit state machine that refuses evaluation without reproduction, publication without a deterministically verified winner, completion without a publication record, or arbitrary/out-of-order state assignment. Failure evidence is bounded and typed; candidate patches, deterministic evaluations, data-contract deltas, and draft-PR publication identity are durable-workflow-safe value objects.
- Expected/observed last commit: Expected task commit `feat: define gated pipeline repair workflow`; observed prior task commit `86889d6 ops: package web and ios client releases`.
- Task dependencies and owned files: `HEAL-T01` starts the auto-heal dependency chain and owns only `loopguard.heal` value contracts, their tests, and the concrete lightweight `heal` dependency extra. Airflow, dbt, and OpenLineage runtimes remain duck-typed and are not pulled into the core install.
- Red/green/regression commands with exit codes and counts: The focused suite first failed during collection because `loopguard.heal` did not exist. It now passes 20 model and optional-dependency metadata tests. Ruff lint and formatting checks pass on the new package/tests, editable `.[heal]` installation resolves current Docker 7.2, PyGithub 2.9, and Temporal 1.31 clients under the declared major-version bounds, `pip check` reports no broken requirements, and all three client imports plus the repair model import succeed.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/models.py`, `loopguard/src/loopguard/heal/__init__.py`, `loopguard/tests/heal/test_models.py`, and the `heal`/`all-dev` metadata in `loopguard/pyproject.toml`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T01` as `feat: define gated pipeline repair workflow`; then begin `HEAL-T02` with hostile, cross-source failure-intake fixtures and fingerprint tests.
