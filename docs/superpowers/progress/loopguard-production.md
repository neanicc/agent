# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 12 of 13 complete — hosted cloud control plane and independently releasable core clients complete; auto-heal implementation active
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-auto-heal-pipelines.md`, `HEAL-T03`
- Last completed step: `HEAL-T03` completed inline. Replay fixtures now infer bounded nested JSON/CSV structure, primitive types, nullability, numeric ranges, privacy-safe enum cardinality, and deterministic schema deltas. Exact identities, secrets, coordinates, timestamps, free text, rare categories, identifiers, and quasi-identifiers are replaced with keyed deterministic type-preserving values; coordinates remain valid but differ from source, string-coordinate failures remain non-numeric, and manifests record transformations without source values. Empty input requires an explicit schema and is marked `schema_synthetic`.
- Expected/observed last commit: Expected task commit `feat: build redacted pipeline replay fixtures`; observed prior task commit `da4cc82 feat: normalize pipeline failure intake`.
- Task dependencies and owned files: `HEAL-T03` consumes normalized T02 evidence but owns only local schema inference, privacy transformations, replay-fixture serialization, and their tests. It never stores the redaction key, raw source values, or exact string enum values in the fixture manifest/schema.
- Red/green/regression commands with exit codes and counts: Fixture tests first failed during collection because the services were absent. All 20 auto-heal contract/intake/fixture tests now pass, plus 9 legacy detector/agent public-API regressions. Strict Ruff and formatting checks pass across the heal package. Tests prove type preservation, coordinate range and perturbation, nested/null schema inference, privacy-safe enum evidence, deterministic transformations, secret removal, non-numeric failure preservation, row/byte/non-finite fail-closed behavior, schema-synthetic provenance, and CSV primitive inference.
- Evidence/artifact paths: `loopguard/src/loopguard/heal/fixtures.py`, `loopguard/src/loopguard/heal/schema.py`, and `loopguard/tests/heal/test_fixtures.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real container execution, Windows service validation, and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents, workflow linting, native release archive, and migration compilation are green.
- Next exact action: Commit `HEAL-T03` as `feat: build redacted pipeline replay fixtures`; then begin `HEAL-T04` with isolated, fingerprint-matching reproduction contracts and hostile sandbox tests.
