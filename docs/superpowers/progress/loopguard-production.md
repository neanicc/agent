# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T06`
- Last completed step: `SURFACE-T06` completed inline. Web sign-in now uses server-side OIDC Authorization Code + fresh S256 PKCE, strict state/nonce/issuer/audience/signature/expiry checks, encrypted Secure/HttpOnly/SameSite sessions, bounded refresh-token rotation, current Next `proxy.ts` route protection, and exact WebAuthn action-challenge/user/origin/RP/user-verification/counter binding. iOS now uses ephemeral `ASWebAuthenticationSession`, fresh PKCE, RS256/PS256/ES256 JWKS verification, Keychain access/refresh rotation, Secure Enclave P-256 signing when available, an encrypted device-only Ed25519 fallback when unavailable, one-use pairing, and local key destruction on logout/revocation. Pairing now registers the public key with cloud action verification, which accepts both P-256 and Ed25519 exact-payload signatures.
- Expected/observed last commit: Expected task commit `feat: add client authentication and device pairing`; observed prior task commit `d404e65 feat: scaffold native loopguard ios app`.
- Task dependencies and owned files: `SURFACE-T06` consumes the T04 action-proof boundary and T05 native foundation, publishes server-only OIDC/WebAuthn proof primitives and native device proof for T07–T09, and keeps browser approval controls fail-closed until the Devices surface persists a registered passkey and supplies its transport. No client secret or private device key crosses a client boundary.
- Red/green/regression commands with exit codes and counts: Web auth/WebAuthn RED failed because both modules were absent. iOS auth/pairing RED failed because all native trust types were absent. Final gates: all 30 Vitest tests, strict TypeScript, zero-warning ESLint, optimized Next build, and all 9 Playwright tests passed; the full Swift Testing suite passed on iOS 26.5 under Swift 6 strict concurrency and warnings-as-errors; focused cloud action/query/OpenAPI tests passed 13 tests with only the recorded external Starlette warning; Ruff passed. The signed-out app installed and launched on iPhone 17 Pro Simulator.
- Evidence/artifact paths: `apps/web/src/auth.ts`, `apps/web/src/security/webauthn.ts`, `apps/web/src/app/auth/`, `apps/web/src/app/(public)/sign-in/`, `apps/ios/LoopGuard/Features/Authentication/`, `apps/ios/LoopGuard/Security/DeviceKeyStore.swift`, `apps/ios/LoopGuard/Features/Devices/DevicePairingView.swift`, `services/control-api/src/loopguard_api/actions.py`, and `/private/tmp/loopguard-t06-sign-in.png`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T06` as `feat: add client authentication and device pairing`; then begin `SURFACE-T07` with failing timeline-deduplication, inbox-priority, native route-inventory, and accessibility/navigation tests.
