# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 9 of 13 complete — hosted cloud control plane complete; control-surface core tranche in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-control-surfaces.md`, `SURFACE-T03`
- Last completed step: `SURFACE-T03` completed inline. The web console now has real Inbox, Runs, Changes, Verification, and Hosts & integrations list/detail routes, capability-safe navigation with Repairs absent, shared loading/empty/error states, local-only first-run setup guidance, and a cursor-replay timeline that queues live gaps until durable replay completes. Browser streaming uses a same-origin CSRF-protected BFF exchange for an origin-bound, one-use upstream ticket; bearer credentials never enter browser JavaScript or WebSocket URLs. The Workbench presentation covers 320/768/1280/1536 layouts, keyboard mobile navigation, labelled mobile table rows, visible focus, semantic status text/icons, progressive evidence disclosure, and coarse-pointer targets.
- Expected/observed last commit: Expected task commit `feat: add replay-safe web session console`; observed prior task commit `71f9775 feat: scaffold typed web control console`.
- Task dependencies and owned files: `SURFACE-T03` consumes the T02 typed BFF and design system and supplies route, async-state, query, and stream primitives to `SURFACE-T04`. Repairs and administration remain absent until their owning capability-aware tasks.
- Red/green/regression commands with exit codes and counts: Timeline RED failed because the reducer was absent; GREEN covers deduplication, cursor gaps, out-of-order replay, and pending-event drain. Stream-ticket RED failed at missing route, then passed two auth/CSRF/isolation tests. Browser QA exposed and regression-tested a receiver-unsafe default `fetch` call plus a missing CSRF meta bridge. Final gate: 11 Vitest tests passed; strict TypeScript, zero-warning ESLint, and production Next build exited 0; 7 Playwright tests passed across 320/768/1280/1536 viewports, keyboard mobile navigation, complete visible-route inventory, capability-hidden Repairs, first-run integrations, and replay reconnect. In-app Browser confirmed a 320px document/body/main width of exactly 320px and a functional full-height navigation dialog.
- Evidence/artifact paths: `apps/web/src/app/(console)/`, `apps/web/src/app/api/stream-ticket/`, `apps/web/src/components/`, `apps/web/src/lib/api/session-stream.ts`, `apps/web/e2e/`, `apps/web/playwright.config.ts`, and `apps/web/src/styles/globals.css`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation and the opt-in Docker/PostgreSQL RLS proof remain external CI requirements; deterministic local equivalents and migration compilation are green.
- Next exact action: Commit `SURFACE-T03` as `feat: add replay-safe web session console`; then begin `SURFACE-T04` with failing explicit target/effect/risk/expiry and expired-action tests.
