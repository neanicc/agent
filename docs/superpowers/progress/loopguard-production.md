# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 5 of 13 complete — tranche 6 model routing and cost control is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T03`
- Last completed step: `ROUTING-T03` completed inline. Versioned, strict TOML rules now match phase/profile predicates and select only fresh, surface-compatible, effort-compatible, capable, context-fit models with known prices. Decisions explain every rejection and fallback, preserve the current model/effort whenever automatic surface control is unavailable or catalog trust is stale, reject malformed/cyclic policy, and deterministically rank by policy priority, exact expected unit cost, then stable model ID.
- Expected/observed last commit: Expected task commit `feat: route managed phases with deterministic policy`; observed predecessor `111538d feat: extract zero-token routing features`.
- Task dependencies and owned files: `ROUTING-T03` consumes immutable T01 catalogs and T02 task profiles. It adds model tags solely as signed/discovered catalog metadata and packages the default policy as wheel data. `ROUTING-T04` next adds strict session/guard/judge/context budgets and a privacy-safe judge cache.
- Red/green/regression commands with exit codes and counts: T03 RED exited 2 during collection with the expected missing `loopguard.router.policy` module. GREEN exited 0 with 14 policy/matrix/fallback/trust/tie-break tests; routing/adapter/control regression exited 0 with 442 tests. Task-owned Ruff lint, formatting, and diff whitespace validation exited 0. A non-isolated wheel attempt correctly failed because the existing venv has setuptools 65.5 below the declared `>=68`; the declared isolated build then exited 0 with setuptools 83 and direct archive inspection confirmed `loopguard/router/default-policy.toml` is packaged.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{catalog.py,policy.py,default-policy.toml,__init__.py}`, `loopguard/tests/router/{conftest.py,test_policy.py}`, and `loopguard/pyproject.toml` package-data declaration.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T03 focused 14 passed, routing/adapter/control regression 442 passed, and isolated wheel/package-data verification passed; T02 focused 10 and routing/verification regression 130 passed; T01 focused 16 passed; preceding full Python suite 653 passed, Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T03` as `feat: route managed phases with deterministic policy`, verify the clean tree and commit subject, then read all of `ROUTING-T04` and begin with failing optional-service budget and judge-cache tests.
