# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 4 of 13 complete — tranche 5 managed integrations is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-agent-integrations.md`, `INTEGRATIONS-T06`
- Last completed step: `INTEGRATIONS-T06` completed inline. Managed Claude now uses a narrow version-pinned Node/TypeScript bridge over bounded JSONL, streaming-input multi-turn sessions, SDK initialization model/effort discovery, `Query.interrupt()`, explicit `canUseTool` permission promises, distinct between-turn injection, normalized usage/cost/events, SDK sandbox fail-closed settings, process cleanup, owner-only durable state, and conservative restart orphaning that never duplicates an in-flight turn. Managed starts require the shared proof/baseline/worktree-lease evidence and only API-key or documented Bedrock/Vertex/Foundry authentication; environment OAuth, SDK-reported subscription login, and provider mismatches fail before the first prompt is queued.
- Expected/observed last commit: Expected task commit subject `feat: manage claude through sdk bridge`; observed predecessor `4297ef9 feat: manage codex through app server`.
- Task dependencies and owned files: `INTEGRATIONS-T06` promotes the proof/baseline/worktree lease evidence to the shared adapter contract, keeps Claude lifecycle and approval semantics isolated from Codex, and owns the pinned SDK bridge plus Python adapter. `INTEGRATIONS-T07` next adds the integration doctor and compatibility matrix across managed and attached surfaces.
- Red/green/regression commands with exit codes and counts: T06 RED exited 2 during collection because `loopguard.adapters.claude_managed` did not exist. Final Python managed-adapter coverage exited 0 with 48 passed; the broader adapter/context/verification gate exited 0 with 110 passed. The pinned Node bridge exited 0 with 3 passed, strict TypeScript exited 0, and `npm audit --omit=dev` reported 0 vulnerabilities. A live no-auth/no-billing stdio smoke returned standards-compliant JSON-RPC `-32601` plus stable `unsupported_method` bridge evidence. Full Ruff, focused format, and diff whitespace checks exited 0, and the final full Python regression exited 0 with 615 passed. Only the recorded external Starlette deprecation remains.
- Evidence/artifact paths: `loopguard/src/loopguard/adapters/{base.py,claude_bridge.py,claude_managed.py}`, `loopguard/tests/adapters/{test_base.py,test_claude_managed.py}`, and `loopguard/integrations/claude-bridge/{package.json,package-lock.json,tsconfig.json,src/index.ts,src/index.test.ts}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate.
- Last successful cumulative verification: 2026-07-15 — full Python suite 615 passed, managed integration related gate 110 passed, Node bridge 3 passed, strict TypeScript clean, production npm audit clean, full Ruff clean, diff whitespace clean, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None.
- Next exact action: Commit `INTEGRATIONS-T06` as `feat: manage claude through sdk bridge`, verify the clean tree and commit subject, then read and begin `INTEGRATIONS-T07` with failing integration-doctor and managed-versus-attached compatibility-matrix tests.
