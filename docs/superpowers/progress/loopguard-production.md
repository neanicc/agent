# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 7 of 13 complete — tranche 8 browser acceleration is in progress
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-browser-acceleration.md`, `BROWSER-T03`
- Last completed step: `BROWSER-T03` completed inline. The Python client launches the pinned broker with an argv array and in-memory one-time capability, validates the version handshake and every correlated bounded response, serializes pending calls, applies exponential restart backoff, hard-times page probes, and kills the supervised process group on invalid response, timeout, close, or startup failure. Health exposes only versions/context counts. BrowserService registers daemon sessions, creates session-scoped contexts on demand, closes them on session stop, and persists only owner-only non-secret lease markers—not context capabilities. Broker generation changes and daemon restart convert active leases to explicit `lost` state so callers reconnect. Node close/page commands now carry explicit session IDs, allowing one authenticated daemon connection to supervise multiple isolated sessions without weakening context ownership.
- Expected/observed last commit: Expected task commit `feat: supervise local browser broker`; observed predecessor `9cd9ee6 feat: reuse browsers with isolated contexts`.
- Task dependencies and owned files: `BROWSER-T03` consumes the T01/T02 protocol/server and registers `browser_client`/`browser_service` in production `DaemonServices`. The service is lazy: daemon startup does not launch or require a browser until browser work is requested. T04 will add encrypted, scoped authentication state and plaintext lease cleanup.
- Red/green/regression commands with exit codes and counts: T03 RED exited 2 with the expected absent Python browser package. GREEN exited 0 with 7 Python tests covering crash restart/backoff, capability rotation, hard timeout, correlation failure, owner-only secret-free lease markers, broker/daemon restart loss, daemon event lifecycle, and registry wiring. The real pinned Python-to-Node smoke initially exposed a missing tsx `--` separator and then an AF_UNIX overlength endpoint; both were root-caused and fixed, after which it passed. Production daemon lifecycle plus browser supervision passed 8/8 under required socket permission. Node regression remains 16/16, strict TypeScript and Ruff are clean, and npm production audit remains clean.
- Evidence/artifact paths: `loopguard/src/loopguard/browser/{__init__.py,client.py,service.py}`, `loopguard/tests/browser/test_client.py`, `loopguard/src/loopguard/control/{daemon.py,lifecycle.py}`, and session-bound updates in `loopguard/integrations/browser-broker/src/{protocol.ts,protocol.test.ts,server.ts}`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — tranche 6 full Python gate 733 passed with only the recorded external Starlette deprecation; T07 router/phase/managed-adapter gate 112 passed; isolated wheel/package-data verification passed; Node bridge 3 passed after the expected sandbox-only IPC denial and permitted rerun, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, and live read-only agent probes stable.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `BROWSER-T03` as `feat: supervise local browser broker`, then begin `BROWSER-T04` with failing scoped-auth ownership and plaintext cleanup tests across Python and Node.
