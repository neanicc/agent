# LoopGuard Production Implementation Progress

- Branch: `feat/loopguard-production`
- Current milestone/tranche: 6 of 13 complete — tranche 7 preference intelligence is next
- Current plan and immutable task ID: `docs/superpowers/plans/2026-07-02-model-routing-cost-control.md`, `ROUTING-T07`
- Last completed step: `ROUTING-T07` completed inline. Explicit plan/implement/verify/repair state now routes only through supported managed turn/query launch boundaries, rejects active-tool and unsafe adapter switches, persists an `applying` route before provider work, and recovers without replay by comparing the observed route. Route disagreement and adapter failure become durable orphaned states for human review. SQLite compare-and-swap updates prevent concurrent coordinators from launching the same phase, and bounded failed-verification repairs cannot loop indefinitely.
- Expected/observed last commit: Expected task commit `feat: route models at managed phase boundaries`; observed predecessor `f960565 feat: evaluate routing in shadow and holdout modes`.
- Task dependencies and owned files: `ROUTING-T07` consumes deterministic routing decisions from T03, budgets from T04, and the managed adapter lifecycle from tranche 5. The coordinator deliberately accepts a phase launcher that creates the next provider-supported turn/query; it never claims unsafe mid-tool model mutation. `PREFERENCES-T01` next owns preference schemas and precedence without changing routing.
- Red/green/regression commands with exit codes and counts: T07 RED was the plan-recorded missing coordinator. GREEN exited 0 with 5 phase integration tests, including crash recovery, route mismatch orphaning, bounded repairs, active-tool rejection, and cross-connection contention. Router, phase, and both managed-adapter regressions exited 0 with 112 tests. Ruff and diff whitespace checks exited 0.
- Evidence/artifact paths: `loopguard/src/loopguard/router/{coordinator.py,__init__.py}`, `loopguard/src/loopguard/adapters/{codex_managed.py,claude_managed.py}`, `loopguard/src/loopguard/control/daemon.py`, and `loopguard/tests/integration/test_phase_routing.py`.
- Completed milestone tags: Tranche 1 `FOUNDATION-T01..T06`; tranche 2 `INTEGRATIONS-T01..T04`; tranche 3 `VERIFY-T01..T07` with prompt-to-proof E2E and the Phase 1 cumulative gate; tranche 4 `CONTEXT-T01..T07` with the 388-test context/control/adapter gate; tranche 5 `INTEGRATIONS-T05..T08` with managed Codex/Claude, capability diagnostics, guided setup, and hardened user-service lifecycle.
- Last successful cumulative verification: 2026-07-16 — T07 router/phase/managed-adapter gate 112 passed; T06 routing/CLI/daemon regression 93 passed; final full Python suite after T04 713 passed; T03 isolated wheel/package-data verification passed; Node bridge 3 passed, strict TypeScript clean, production npm audit clean, Ruff lint clean, generated CLI docs clean, live read-only agent probes stable, and only the recorded external Starlette deprecation.
- Known baseline failures: No LoopGuard-owned baseline failures remain. One external `StarletteDeprecationWarning` from FastAPI/TestClient is recorded and is not suppressed. Repository-wide `ruff format --check .` reports the recorded pre-existing unrelated formatting baseline and is not silently rewritten during managed-integration tasks.
- Active blockers: None. Real Windows service validation remains an external CI requirement and the CLI labels that path experimental.
- Next exact action: Commit `ROUTING-T07` as `feat: route models at managed phase boundaries`, run the tranche 6 cumulative gate, then read all of `PREFERENCES-T01` and begin with failing layered-precedence schema tests.
