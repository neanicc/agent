# acme-data (sandbox)

A small sample repo for ad-hoc agent tasks. Real files you can ask an agent to read:

- `pyproject.toml` — project name & version
- `config.json` — service configuration
- `data/customers.csv` — sample data
- `src/pipeline.py` — the data pipeline
